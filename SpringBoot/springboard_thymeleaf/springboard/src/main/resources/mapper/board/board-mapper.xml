<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/dtd/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.springboard.mapper.BoardMapper">
    <!--<select id="getBoard"
    parameterType="int"
    resultType="com.bit.springboard.dto.BoardDTO">
        SELECT BOARD_NO
             , BOARD_TITLE
             , BOARD_CONTENT
             , BOARD_WRITER
             , BOARD_REGDATE
             , BOARD_CNT
            FROM T_BOARD
            WHERE BOARD_NO = #{boardNo}
    </select>-->
</mapper>